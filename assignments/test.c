#include<stdio.h>
 int main()
 {
    
     char ch;
     printf("%d",sizeof(ch));
     if(-1)
        printf("yes");
     if((~sizeof(ch) + !sizeof('A')))
           printf("Hinjewadi");
       else
          printf("MarketYard");
      return 0;
}